# Claude Code Instructions

## Branch workflow

- Always work on the `development` branch
- Never commit or push directly to `main` or `staging`
- All commits go to `development` only
- Merging to `main` is done manually by the user
